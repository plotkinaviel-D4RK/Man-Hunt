Place character source images and generated 3D models here.

Suggested structure:

- `source/` for the original 2D character image
- `models/` for exported `.glb` or `.gltf` files

Example future files:

- `source/profile-character.png`
- `models/profile-character.glb`
